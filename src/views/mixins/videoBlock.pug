mixin videoBlock( video = {} )
  .videoBlock 
    a(href=routes.videoDetail(video.id))
      h4.videoBlock__title=video.title
      h6.videoBlock__views=video.description
      video.videoBlock__thumbnail(src=video.videoFile, controls, autoplay)